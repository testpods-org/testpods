[project]
name = "testpods-agent"
version = "0.1.0"
description = "Python scripts for the TestPods Agent Claude Code plugin"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["plugin/scripts", "plugin", ".", "scripts"]
